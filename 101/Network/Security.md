## Introduction to tcpdump

- Install tcpdump

```bash
$ sudo apt-get install tcpdump
$ sudo tcpdump -i lo
$ sudo tcpdump [option] [expression]

$ ip link show
$ tcpdump -D

$ sudo tcpdump -i lo # Monitor the loopback address
$ sudo tcpdump -i 3 # Monitor the loopback address


```
